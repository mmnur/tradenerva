# TradeNerva Public Homepage

Static marketing/showcase homepage for the public `mmnur/tradenerva` repository.

## Local Preview

Open `index.html` directly in a browser, or serve this folder with any static file server.

## Test

```bash
node tests/homepage.test.mjs
```

## Publish Later

When ready, copy these files into the public repository and enable GitHub Pages or another static hosting provider.
